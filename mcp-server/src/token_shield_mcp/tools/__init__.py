"""The nine wave-1 tools, one module each, per the MCP design spec's tool
list. Every tool is a thin wrapper over an already-tested scripts/ function;
see the individual modules for which one and why."""
