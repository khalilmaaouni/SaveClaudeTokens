# Task packet T4.2: the first screen wording

**Format note.** Same as packet T2.1: the founder plan's section 19 is not
present on this machine, so this packet follows the ratified subagent brief
standard plus the WBS task table columns. Flip condition is identical.

You are executing this alone. You cannot see the conversation that produced it.

## Assignment

| Field | Value |
|---|---|
| Task ID | T4.2 |
| Owner role | A8, surface and wording |
| Model | sonnet. Rationale: scoped implementation and user facing wording from a settled spec. Not haiku, because the label discipline below is a correctness constraint and not a mechanical edit. |
| Depends on | nothing. Runs in parallel with T2.1, whose files are disjoint from yours. |
| Branch | new branch off `main`, then a pull request. Never a direct commit to main. |

## Goal

A brand new user runs the trial and, in the first three lines, learns one
number about their own usage and one thing to do about it, in plain language.
Today they get a banner, a legend explaining the label vocabulary, and then a
column of `MEASURED` lines. That is a correct screen written for someone who
already knows the product.

The sixty second promise is E4's, and this task owns the wording half of it.
T4.1 owns the timed proof and is a separate task on separate files.

## Read these first

1. `scripts/trial.py`, whole file, 7.3 KB. The output block starts around line
   57 and the current lead lines are at 82 and 86.
2. `commands/start.md`, step 2 specifically.
3. `docs/plan/2026-08-15-LEADERSHIP-WBS.md`, section 2 item 1, which states the
   promise you are implementing: one hero number, one sentence of diagnosis, one
   recommended action.

## Files you own, and may edit

- `scripts/trial.py`
- `commands/start.md`

## Files you may not touch

`scripts/measure_tokens.py`, `scripts/token_shield.py`. Everything not in your
owned list is untouchable by default.

## Constraints, all of them binding

- **Never change a confidence label.** `MEASURED`, `NATIVE`, `VERIFIED`,
  `ESTIMATED` and `RECOMMENDED` keep their exact spelling, their exact meaning,
  and their attachment to the exact numbers they carry today. This is the
  project's hardest invariant: the labels never merge and no number changes
  label. You are reordering and rewording the prose around them, never the
  labels themselves.
- **`NATIVE` stays attributed to Anthropic and never appears in dollars.**
- **No cross label totals.** Do not add a line that sums a MEASURED number and a
  NATIVE number, however tempting a single headline is.
- **The NO DATA paths at lines 57 and 74 keep working and keep leading.** A user
  with no transcripts must still get the NO DATA line first, not a hero number
  computed from nothing.
- **No em dashes and no en dashes anywhere.** Use commas, colons or parentheses.
- **No AI vendor attribution anywhere.** Sole author: Khalil Maaouni.
- **Jargon out of the lead.** "Startup floor share" and "cache hit ratio median"
  are correct and stay available further down. They are not the first thing a
  new user should read.
- **Red before green.** If you change what a test asserts, see it red first.

## The shape to aim for

Three lines before anything else, then the existing detail unchanged beneath:

1. One hero number about the reader's own usage, carrying its label.
2. One sentence saying what that number means for them, in plain words.
3. One recommended action, naming the exact command that performs it.

The legend explaining the label vocabulary moves below those three lines. It is
useful and it is not the first thing anyone needs.

## Done-check, run after your last edit, output quoted in the pull request

```bash
cd /Users/khalil.maaouni/SaveClaudeTokens/scripts && python3 test_trial.py
```

Exits 0 after the wording change.

Then the whole documented suite from the repository root (the long
`check_py311.py` line in `CLAUDE.md`, ending with `test_architecture.py`),
because `commands/start.md` is checked by more than one suite.

If T4.1's install smoke has landed by the time you finish, run it too:

```bash
cd /Users/khalil.maaouni/SaveClaudeTokens/scripts && python3 test_install_smoke.py
```

If that file does not exist yet, say so in your report. Do not create it: it
belongs to T4.1 and another writer owns it.

## Response format

Return, and nothing else:
1. The branch name and one line on what changed.
2. The first six lines of the new trial output, verbatim.
3. The done-check command and its last ten lines, verbatim.
4. One line confirming every confidence label kept its exact spelling and its
   exact number.
5. Anything you could not do, with the reason.

Hard cap 600 words.
