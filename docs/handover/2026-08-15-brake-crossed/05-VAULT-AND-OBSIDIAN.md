<!-- GENERATED by tools/bm_handover.py. Regenerate rather than hand-edit, except inside the human markers, which are preserved verbatim. -->

# Vault and Obsidian

Update the memory vault session log to the close moment (rule step 6): record what changed, what was learned, and what is open.

This page has no store-derived section: the vault lives outside this project's own store.

## Notes (human, preserved verbatim on regeneration)

<!-- bm-human:begin -->
Vault: `/Users/khalil.maaouni/Documents/Kay Vault`.

Token Shield still has NO dedicated vault space; general session logs carry it, which is recorded in
`PROJECT.md` under "Vault space: none yet". That remains true after this session and is worth
settling: this project now has enough failure history of its own to deserve a space.

**Session log owed** in the SaveClaudeTokens sessions folder, covering: the spend brake crossing
(867,571 against 800,000), PR 76 open and unreviewed, the suite moving 464 to 504, and the four
mistakes in 02-LEARNINGS-AND-MISTAKES.md.

**Failures-Index candidates from this session**, each a genuinely new shape rather than a repeat:

- *A ceiling measured once is measured too late.* Two consecutive sessions overran the same brake,
  the second by six times the first, while holding the first one's written warning. The warning was
  read and not operationalised, which is the actual failure: a rule that is not measured before each
  dispatch is not a control.
- *A published verification command that was never run.* Correcting a false security claim, I
  published two greps and described their output without executing them; both descriptions were
  wrong. Sibling of the existing "the sweep you wrote is not the control" note, but distinct: here
  the unrun check was being published TO third parties as the proof of a security property.

**Existing Failures-Index entries this session re-confirmed** and did not need to relearn:
`dirty-worktrees-hold-unmerged-drafts` (both worktrees status-checked before removal),
`measuring-a-tree-you-are-still-editing` (baseline taken in a clean worktree of `origin/main`), and
`tests-written-backwards-from-the-fix` (caught one of my own, calibrated it by reinjection).
<!-- bm-human:end -->
