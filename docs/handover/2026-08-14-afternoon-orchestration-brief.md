# Afternoon orchestration brief: parallel program under a Fable orchestrator

Written by session f3e57169 at its 640k dispatch line. The next session starts fresh in ~/SaveClaudeTokens, reads STATE.md first, states its 800k ceiling, and runs THIS program. Two writer lanes maximum in the repo, fences before dispatch, workers never push, every merge preceded by orchestrator reinjection, refute reviews on judgment surfaces, absolute paths in every worktree command.

## The lanes, with models declared and why

- LANE A, wave R + WR+ (the native reduction engine, size L): sonnet builder in a worktree (scoped implementation from the plan in docs/superpowers/plans/2026-08-13-solid-core-waveR-plan.md). Step 1 is the interlock commit FIRST: no apply while any experiment is open, and claude-md-diet-v2 IS open, so R lands buildable with its apply path locked, tested by fixture only. Opus refuter mandatory before merge (trust-changing: it writes user config when unlocked).
- LANE B, Signals S1 (schema, local rollup, outbox, preview; zero network, size M): sonnet builder in a worktree per docs/superpowers/plans/2026-08-14-token-shield-signals-plan.md. Opus PRIVACY refuter mandatory (briefed to link two reports or leak a field; the spec says how).
- RIDER 1, scorecard re-score (judgment, small): opus agent, read-only plus docs/SCORECARD.md single-file fence. Re-score rows against the merged 1.8.0 evidence (PRO2 personalization, TOUR1 selection, METR1 attribution, HIST1 update resilience, DASH1/H2 UX) with a grep-verifiable citation per cell; append to history with the 1.8.0 tag. OWED from the release train (skipped during the tag run, recorded as a decision then, repaid here).
- RIDER 2, vault bookkeeping (mechanical): haiku agent. Update Kay Vault Home latest-sessions list and saveclaudetokens Overview with the 1.8.0 release facts from STATE.md; add two failure-index lines (stacked-merge routing miss; git checkout on uncommitted work). Exact wording in STATE.md DAY CLOSED block; haiku copies, never composes.
- Sequencing: A and B run parallel (disjoint fences). Riders run alongside (different trees or single-file fences). Fleet F1 starts only after Signals S1 merges or if lane A finishes early; never a third repo writer lane.

## Standing constraints the fresh session must re-read, not trust from here

STATE.md (fences, incident rules, parked branch-cleanup one-liner), the release gate history in docs/ROADMAP.md, push gates via the github-desktop-push skill, no dashes, no attribution, labels never blend, NO DATA beats a guess, commands capped at 6, zero hooks by default.

## Done means

Each lane: done-check run after the last edit and quoted, defect reinjected by the orchestrator before merge, PR merged (founder present) or queued, GANTT ticked and republished at 89b7616a, vault log updated at close.
