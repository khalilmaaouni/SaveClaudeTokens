<!-- GENERATED by tools/bm_handover.py. Regenerate rather than hand-edit, except inside the human markers, which are preserved verbatim. -->

# Next loops, prioritized

## Candidates already recorded by the store

No unacknowledged handover exists to seed a next loop from; the priority order below is entirely by hand.

## Notes (human, preserved verbatim on regeneration)

<!-- bm-human:begin -->
1. Execute ADVISOR-PLAN.md in a fresh session. Step 0: refresh GANTT and PROJECT.md, republish the progress page. Step 1: verification sweep (sonnet, read-only) over the ten unverified blueprint facts; cut what fails. Then v1.7 build per the dispatch table (sonnet builders, haiku sweeps, opus adversarial review of cohort math and net accounting, Fable judges only). Budget 300K output soft, 500K hard.
2. Reinstall the plugin on this machine under the new ids (claude plugin uninstall save-claude-tokens; marketplace add khalilmaaouni/token-shield; install token-shield@token-shield), then update the three stale lines in ~/.claude/CLAUDE.md.
3. Run the first real experiment to fill the VERIFIED column: python3 scripts/cli.py experiment start "claude-md-diet", apply the diet (checkpoint at ~/.claude/token-baselines/2026-08-12-phase4-diet-plan.md), work normally, end across a reload.
4. Small honest fix in the meter: measure_tokens.py lines 100 and 145 silently skip unreadable files and truncated lines; count and report skips (brothersbe silent-failure lint finding).
5. Weekly reviews are overdue for both brothermode and brothersbe (7 days); run tools/WEEKLY-REVIEW.md each.
<!-- bm-human:end -->
