<!-- GENERATED by tools/bm_handover.py. Regenerate rather than hand-edit, except inside the human markers, which are preserved verbatim. -->

# Rules and process fixes

## bm_store.py verify() problems at generation time

None. If a process fix belongs here anyway, it is one this session found some other way; write it in below.

## Notes (human, preserved verbatim on regeneration)

<!-- bm-human:begin -->
PROPOSED (constitution is founder-owned; only the founder lands these):
1. The baton ceremony triggers on every brothermode CLOSE, not on session end. A session that invokes /brothermode and then continues with other work still owes the ceremony at the moment that brothermode loop closes. This session violated exactly that seam.
2. Public-repo packs: the ceremony says commit the pack folder in the project repo. For a PUBLIC repo the pack carries machine-local detail, so this session kept the pack at docs/handover/ excluded via .git/info/exclude. FOUNDER RULED 2026-08-12 at this close: for public repos the pack's durable git home is the Kay Vault copy (vault hooks snapshot to git) plus the zip in BrotherModeUp-handovers; the pack is never committed to the public repo. verify-close therefore ends FAIL on that one named check, and that FAIL is the ruled, recorded outcome, not an oversight. Amendment for the ceremony spec goes to the pending list; the constitution is founder-owned.
3. Store bootstrap: the skill's session-start hint points at the clone's bm_store.py (schema 18) while the ceremony tools read schema 20; a fresh init immediately hits schema-behind. Fix: the hint should point at the BrotherModeUp tools, or the init should migrate.
<!-- bm-human:end -->
