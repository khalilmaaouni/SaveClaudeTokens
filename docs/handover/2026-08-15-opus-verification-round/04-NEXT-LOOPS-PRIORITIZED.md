<!-- GENERATED by tools/bm_handover.py. Regenerate rather than hand-edit, except inside the human markers, which are preserved verbatim. -->

# Next loops, prioritized

## Candidates already recorded by the store

- follow up on handover `f28881a3`: [WITHHELD: 72 chars of founder text]

## Notes (human, preserved verbatim on regeneration)

<!-- bm-human:begin -->
## 1. D22, the tests that passed for the wrong reason

Take this before the remaining Majors, even though it fixes no user-visible defect. Every Critical closed today was found by a reviewer and none by the suite. Named cases: the share card's latest-wins test used two VERIFIED rows, which is the exact shape the supersede defect hides behind; the trial's read-only test greps source text and misses append mode and write_text; one no-data fixture is never parsed at all because it fails a substring gate; and the dashboard suite reported 23 passed with four Criticals live.

Fixing defects without fixing the tests that missed them just resets the trap.

## 2. D16, NATIVE overstated when cache writes carry no TTL split

The row the market position rests on. Cache writes with an unknown TTL are never charged as premium, while the code already knows that data is unpriceable and sets normalized input to NO DATA for exactly that reason. Overstates by roughly six percent at the cheapest TTL and up to twenty-nine at the most expensive. Not reproducible live on this machine, which has no unsplit writes, so it is a schema-version path.

## 3. D17, the trial and the command it recommends disagree

The trial takes the maximum lever, the follow-on command sums them. A stranger sees 216.9M and then 228.2M for the same data, in the same words, within a minute. Summing overlapping levers double counts anyway, so make both take the maximum.

## 4. BX3, the Waste Score

Already in flight in another session's branch, with its formula document written first, which is the sequencing that matters. A score whose formula arrives afterwards is marketing; a score whose formula arrived first is a standard.

## 5. BX1b, finish the zero install promise

A root pyproject.toml with an entry point would make the trial a true one-liner via uvx with nothing left behind. It needs no package index, so it stays inside the release gate. Check first that a packaging file at the root does not confuse plugin loading, since this repository is also a plugin and its own marketplace.

## Then

Fleet F4 (org profile with soft budgets, where an unreachable store never gates a developer and a breach advises rather than blocks) and F5 (signed receipts). Signals stays held at S2. LAB1 stays parked until the verdict.

## The immovable date

claude-md-diet-v2 reaches its verdict near 2026-09-13. Publish it either way. An honest NOT_PROVEN published with its method beside it is the product's best advertisement, and is the thing no competitor in this field currently does.
<!-- bm-human:end -->
