<!-- GENERATED by tools/bm_handover.py. Regenerate rather than hand-edit, except inside the human markers, which are preserved verbatim. -->

# Rules and process fixes

## bm_store.py verify() problems at generation time

None. If a process fix belongs here anyway, it is one this session found some other way; write it in below.

## Notes (human, preserved verbatim on regeneration)

<!-- bm-human:begin -->
## Landed this session

Handovers deliver as ONE zip containing every file they refer to, never the pack beside its parts. Founder directive, merged into BrotherMode's core and its session-start digest as PR 41. Stated NOT ENFORCED, because nothing computes whether a delivery was one file or several.

test_trial.py and test_fleet_dashboard.py are wired into both CLAUDE.md's suite line and ci.yml. When a fence excludes the file that wires a test into CI, grep for the gap immediately after the merge: it happened to BX1 and was caught only because a review had just flagged the same class elsewhere.

## Proposed, for the founder to decide

Track docs/handover/ in git. It is excluded on this machine only, via .git/info/exclude line 12, yet STATE.md points future sessions at files inside it, so that pointer is a promise the repository cannot keep. These are project history rather than machine state. A durable copy is archived either way.

A --strict mode for the integrity self-check that treats SKIP as failure. Today a hand-edited live install, the case where tampering matters most, is the case the check declines to judge. The forgiving default is right for interactive use; an automated check should be able to demand a real answer.

## Rules that earned their keep today

Reproduce a reviewer's findings yourself before accepting them, and re-run their attacks against the fix. This found a further defect inside a fix again today: the path scrub covered the page footer but not the error rows, which is where the account name actually leaked.

Verify in a fresh clone of the remote, not in a local tree. My checksum break was invisible from the working copy, because another session had regenerated the manifest locally, and visible only from a clean clone of the remote.
<!-- bm-human:end -->
