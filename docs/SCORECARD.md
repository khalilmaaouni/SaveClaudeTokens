# Capability scorecard (the yardstick for "nothing under 8.5")

Founder decision 2026-08-13: external benchmark tables are direction, never measurement. This file is the yardstick. Every cell scores ONLY against named evidence in this repo or measured on this machine; a cell without evidence says NOT YET SCORED, never a guess. Re-scored at every release; history kept below.

## Scoring anchors (what a number is allowed to mean)

- 9 to 10: capability proven by calibrated tests AND a measured or verified number from real usage.
- 7 to 8.5: capability shipped and tested; real-usage evidence partial.
- 5 to 6.5: capability exists but is advisory, manual, or unproven on real data.
- Under 5: capability absent or aspirational.
A score may never exceed what its named evidence supports. NO DATA rows stay NOT YET SCORED.

Tie-breaks, so two honest reviewers land on one number: evidence sitting between two bands scores the LOWER band. A cell may reach 8.5 or higher only when it cites BOTH a calibrated test AND a real-usage measured number. Thin-but-present real-usage evidence (fewer than 30 days or fewer than 30 sessions behind the number) caps the cell at 8.0. Ties always break down, never up.

## Current scores (as of 1.8.0, released, re-scored 2026-08-14)

Second evidence-backed pass, scored against the merged 1.8.0 train (main 709fa1b). Every score below cites a calibrated test (grep-verifiable def), a measured number with its source command (re-run read-only, output quoted), or a shipped surface named by file and line. The VERIFIED experiment ledger is still empty: the ledger at `~/.claude/token-shield/savings.jsonl` now holds exactly one verdict, and `python3 scripts/experiment.py report` prints "claude-md-diet 1 runs 0 VERIFIED 1 NOT_PROVEN latest floor reduction -10,670 tokens/call", with `~/.claude/token-shield/experiments/claude-md-diet-v2.json` still open. That NOT_PROVEN is the product working, a refusal to claim what the data does not carry, but it is not proof of a delivered saving, so Actual reduction and Attribution stay capped in the 5 to 6.5 band by the same absence the 1.7.1 pass named. The 1.7.1 pass looked for the ledger under `~/.token-shield/experiments`; the real path is `~/.claude/token-shield` (scripts/experiment.py:52-53), which changes where a reviewer looks, not the verdict count.

| Capability | Score | Evidence (required before any number) |
|---|---|---|
| Actual reduction | 6.0 | Unchanged from 1.7.1, and the reason is citable: the reduction engine itself did not move in this train, `git log 1.7.1..HEAD -- scripts/optimize.py` returns nothing. Calibrated: scripts/test_optimize.py::test_propose_keeps_every_hard_section_verbatim (6 passed, exit 0); scripts/test_experiment.py (35 passed, exit 0) holds the cohort, window and fingerprint guards that decide a verdict. New real-usage evidence exists but is not evidence of a saving: `python3 scripts/experiment.py report` printed "claude-md-diet 1 runs 0 VERIFIED 1 NOT_PROVEN latest floor reduction -10,670 tokens/call", and the ledger record carries "direction": "regression". One closed cycle proves the machinery runs end to end on real sessions; it does not prove a reduction. VERIFIED count across every label is 0, so the honest cap holds and the score stays where the 1.7.1 pass put it. |
| Measurement truth | 9.5 | Calibrated: scripts/test_measure_tokens.py (9 passed), scripts/test_reconcile.py (10 passed), scripts/test_pricing.py (7 passed), exit 0. New in 1.8.0 and machine independent: bench/generate_corpus.py computes every expected counter by direct arithmetic on the values it just wrote, before the meter reads the files, and `python3 bench/run_benchmark.py --corpus /tmp/bench-corpus` printed "ALL PASS (93 checks)"; bench/test_bench.py (3 passed) proves the kit catches a broken corpus instead of rubber-stamping it. Real-usage measured number, independent parser: `python3 scripts/reconcile.py --days 30` printed "sessions parsed independently: 3619" and "RECONCILED: every compared field drifts under 0.5 percent". Not 10: reconcile.py still cross-checks exactly three fields against real transcripts (first_request_median, first_request_share_median, multi_model_count), and a constructed corpus is a math check, not a second independent read of real data. |
| Attribution | 6.5 | Calibrated, and hardened in this train: scripts/test_tools.py::test_dashboard_attributes_the_saving_to_native_caching, ::test_dashboard_shows_no_native_bars_or_dollars_only_a_methodology_pointer, ::test_waterfall_never_sums_percentages_or_marginal_deltas, ::test_waterfall_confidence_labels_never_blend_on_one_line, ::test_verified_headline_is_per_label_and_never_a_cross_label_total and ::test_verified_row_with_differing_fingerprint_renders_historical_with_reason (all inside the 36 passed test_tools.py run, exit 0). Shipped refusal, exercised on this machine's real ledger: `python3 scripts/share_card.py` printed "REFUSED: ledger holds only NOT_PROVEN record(s) (1); nothing VERIFIED or MEASURED to show" (scripts/share_card.py:54 select_card_row, scripts/test_share_card.py 8 passed). scripts/report.py:275 _reverdict gives NOT_PROVEN "revisit" and never a firm verdict; scripts/report.py:325 _what_did_not_work files that record under what did not work rather than dropping it (scripts/test_report.py 12 passed). Still no real-usage proof of a delivered saving to attribute (VERIFIED count 0 across every label), so the cell stays inside the 5 to 6.5 advisory band: the discipline improved, the proof did not arrive. |
| Cache plus startup | 8.5 | Calibrated: scripts/test_profile.py (19 passed, exit 0) now covers the 1.8.0 pressure metrics, including ::test_tool_result_bytes_helper alongside ::test_idle_gap_bucketing_math and ::test_model_switch_detection; scripts/test_detail_report.py::test_schema_v1_top_level_and_leaf_shape (6 passed). The cache hit ratio math is now checked against constructed truth by bench/run_benchmark.py (per-session "hit_ratio" rows and "aggregate:hit_ratio_median" inside the 93 PASS run). Real-usage measured number: `python3 scripts/cli.py profile` printed "sessions in window: 3,619 over 30 days", "first-request floor: 85,736 tokens median, 0.367 share of everything read", "cache hit ratio median: 0.913", plus the new pressure block "duplicate tool reads (total): 5,542". Held at 8.5, not raised: the exact gap the 1.7.1 pass named is unchanged, reconcile.py still reconciles three fields on real data and the cache hit ratio is not one of them. Evidence sitting between two bands scores the lower band. |
| Personalization | 8.0 | Calibrated: scripts/test_advisor.py (49 passed, exit 0), including ::test_companion_ownership_suppresses_duplicate_card, ::test_tournament_ranks_two_candidates_winner_and_visible_loser and ::test_deciding_criterion_always_names_the_first_differing_tuple_field; scripts/test_discover_companions.py (33 passed); scripts/test_doctor.py (31 passed). Real usage against this machine's own install: `python3 scripts/doctor.py` printed "plugin cache: 71 entries", three curated companions "installed and active" (ponytail, caveman, token-saver) and four real "SHARED HOOK" overlaps; `python3 scripts/cli.py advise` printed cards citing this machine's own numbers ("Your model switch session share is 27%", "Your startup floor share is 37%", "Your output tokens total is 252,416,771"), each marked "evidence: ESTIMATED". Shipped and tested, real-usage evidence partial: the 7 to 8.5 band. Capped at 8.0, not 8.5, because the companion half of the personalization rests on a single machine snapshot, the thin-evidence case the anchors cap at 8.0, and the benefit is still ESTIMATED, never proven. |
| Automation | 4.0 | Zero hooks is still the shipped default and is citable three ways: .claude-plugin/plugin.json carries no hooks key, the repo has no hooks/ directory, and CLAUDE.md:27 plus README.md:53 both say so; scripts/session_end_telemetry.py:5 still reads "OPT IN. This plugin does not register this hook for you. Nothing here runs" unless wired by hand (scripts/test_tools.py::test_telemetry_never_breaks_the_session, in the 36 passed run). What 1.8.0 adds is an agent-callable surface, not an autonomous one: mcp-server/src/token_shield_mcp/server.py:47-55 registers nine tools, so a session can pin a baseline, close an experiment and record a decision without a human typing a CLI command (mcp-server/test_mcp_server.py, 13 passed). It is invocation, not action: mcp-server/src/token_shield_mcp/wrappers.py call_printing only captures the stdout of the same scripts, scripts/doctor.py:379 report() prints and changes nothing, and optimize.py --apply stays a separate explicit invocation. Nothing runs unattended and nothing rewrites a config on its own say-so, so this stays in the under-5 band, one step up for the removed human step. |

## External benchmarks on record (UNVERIFIED, direction only)

- 2026-08-13, source unnamed, single-sourced: capability table scoring Token Shield across 13 rows (screenshots held by the founder; sub-8 rows mapped to roadmap items in the vault's Open-Items note).
- 2026-08-13, same source: live-products comparison scoring Token Shield 1.6 at reduction 5.5, measurement 9.5, attribution 6.0, cache plus startup 9.0, personalization 2.5, automation 3.5. Recorded verbatim as received; none of these numbers re-derived here.

## History

- 2026-08-13, no release tag yet: pre-release scoring pass, wave T merged. First evidence-backed pass: Actual reduction 6.0, Measurement truth 9.0, Attribution 6.0, Cache plus startup 8.5, Personalization 6.5, Automation 3.0. Full test suite green (exit 0) and `python3 scripts/reconcile.py --days 30` reconciled on 3,657 real sessions; VERIFIED experiment ledger still empty on this machine.
- 2026-08-14, release tag 1.8.0 (main 709fa1b): re-score against the merged 1.8.0 train. Actual reduction 6.0 (held, optimize.py untouched in the train), Measurement truth 9.0 to 9.5, Attribution 6.0 to 6.5, Cache plus startup 8.5 (held, the reconcile gap it names is unchanged), Personalization 6.5 to 8.0, Automation 3.0 to 4.0. Documented suite green (exit 0), `python3 bench/run_benchmark.py` printed "ALL PASS (93 checks)", mcp-server 13 passed, `python3 scripts/reconcile.py --days 30` reconciled on 3,619 real sessions. The proof ledger now carries exactly one verdict, claude-md-diet NOT_PROVEN with direction regression, and claude-md-diet-v2 is open, so the VERIFIED count is still 0 and Actual reduction and Attribution stay capped by that absence.
